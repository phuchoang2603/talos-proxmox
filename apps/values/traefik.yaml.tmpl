service:
  type: LoadBalancer
  spec:
    loadBalancerIP: ${IP_INGRESS}
tlsStore:
  default:
    defaultCertificate:
      secretName: wildcard-tls
ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.${SSL_LOCAL_DOMAIN}`)
    entryPoints:
      - "web"
      - "websecure"
